export class Hitbox{constructor(owner,damage,radius,angle,knockback=0){this.owner=owner;this.damage=damage;this.radius=radius;this.angle=angle;this.knockback=knockback}canHit(target){const dx=target.x-this.owner.x,dy=target.y-this.owner.y,d=Math.hypot(dx,dy);if(d>this.radius)return false;const a=Math.atan2(dy,dx),diff=Math.atan2(Math.sin(a-this.angle),Math.cos(a-this.angle));return Math.abs(diff)<=Math.PI*.48}}